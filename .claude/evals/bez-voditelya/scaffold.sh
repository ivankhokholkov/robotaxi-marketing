#!/usr/bin/env bash
# СГЕНЕРИРОВАН из evals/fixtures/. Правь фикстуру и перегенерируй: python3 evals/fixtures/build.py
# Ставит профиль в изолированный HOME песочницы и создаёт скелет рабочей папки.
# Без него гейт профиля останавливает кейс — гейт проверяется отдельно.
set -euo pipefail
DEST="${CLAUDE_PROJECT_DIR:-.}"
WORKDIR="$PWD"
mkdir -p "$DEST"
cat > "$DEST/CLAUDE.md" <<'PROFILE_EOF'
# Профиль проекта — ФИКСТУРА ДЛЯ ТЕСТОВ

> ⚠️ Значения не настоящие. Профиль существует только для прогона тестов.

## Рабочая папка проекта

- **Путь:** __WORKDIR__

## Продукт

- **Название сервиса:** ТЕСТ-СЕРВИС
- **Статус названия:** рабочее, в публичных материалах не использовать
- **Города пилота:** Москва, Санкт-Петербург (рекламная кампания — только Москва)
- **Дата старта пилота:** 01.09.2026

## Правовой режим

- **Режим ЭПР:** не знаю, уточняется у юриста
- **Категория ВАТС:** не подтверждено
- **Согласованные зоны:** не подтверждено

## Тарифы

- **Модель тарификации:** два тарифа под сегменты
- **Названия тарифов:** Чилл (18-29), Сити (30-45) — оба naming TBD
- **Что входит:** Чилл — первые 15 минут бесплатно, подача 10 минут. Сити — подача 5 минут. Не снято расхождение: в УТП заказчика общий коммит подачи менее 5 минут противоречит десяти минутам Чилл

## Экосистема и партнёрства

- **Принадлежность к экосистеме:** не разглашается
- **Интеграции, которые можно называть публично:** никакие

## Границы NDA

- **Можно называть публично:** город пилота, тип сервиса, общие бенефиты комфорта и безопасности
- **Нельзя называть, идёт под плейсхолдер:** число машин, себестоимость поездки, целевые CSAT и NPS, партнёры, внутренние сроки
- **Обезличивать перед отправкой во внешние модели:** тарифная сетка, целевые метрики, число машин

## Кто работает с системой

- **Основной пользователь:** тестовый прогон, не человек
- **Кто утверждает материалы:** заказчик
PROFILE_EOF
sed -i '' "s|__WORKDIR__|./|" "$DEST/CLAUDE.md" 2>/dev/null || sed -i "s|__WORKDIR__|./|" "$DEST/CLAUDE.md"
mkdir -p ./{knowledge/{market,competitors,regulation,audience,barriers,references},РЕЗУЛЬТАТЫ/{00_брифы,01_аудитория,02_утп,03_рынок,04_реклама,05_аналитика,06_контент,07_презентации,08_видео},ЛЕНДИНГ/{БЛОКИ,КОМПОНЕНТЫ},БАННЕРЫ/{МАКЕТЫ,brand}}
echo "профиль установлен: $DEST/CLAUDE.md"
